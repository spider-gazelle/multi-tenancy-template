<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Authorize <%= HTML.escape(client_name) %></title>
  <style>
    :root {
      --primary-color: #4f46e5;
      --primary-hover: #4338ca;
      --danger-color: #dc2626;
      --danger-hover: #b91c1c;
      --bg-color: #f3f4f6;
      --card-bg: #ffffff;
      --text-color: #1f2937;
      --text-muted: #6b7280;
      --border-color: #e5e7eb;
    }
    
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }
    
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      background-color: var(--bg-color);
      color: var(--text-color);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 1rem;
    }
    
    .consent-card {
      background: var(--card-bg);
      border-radius: 12px;
      box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
      max-width: 420px;
      width: 100%;
      padding: 2rem;
    }
    
    .header {
      text-align: center;
      margin-bottom: 1.5rem;
    }
    
    .app-icon {
      width: 64px;
      height: 64px;
      background: var(--primary-color);
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 1rem;
      color: white;
      font-size: 1.5rem;
      font-weight: bold;
    }
    
    h1 {
      font-size: 1.25rem;
      font-weight: 600;
      margin-bottom: 0.5rem;
    }
    
    .subtitle {
      color: var(--text-muted);
      font-size: 0.875rem;
    }
    
    .user-badge {
      background: var(--bg-color);
      padding: 0.5rem 1rem;
      border-radius: 20px;
      display: inline-block;
      font-size: 0.875rem;
      margin-top: 0.75rem;
    }
    
    .permissions {
      border-top: 1px solid var(--border-color);
      border-bottom: 1px solid var(--border-color);
      padding: 1.5rem 0;
      margin: 1.5rem 0;
    }
    
    .permissions-title {
      font-size: 0.875rem;
      font-weight: 500;
      margin-bottom: 0.75rem;
      color: var(--text-muted);
    }
    
    .permissions ul {
      list-style: none;
    }
    
    .permissions li {
      padding: 0.5rem 0;
      font-size: 0.9375rem;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }
    
    .permissions li::before {
      content: "✓";
      color: var(--primary-color);
      font-weight: bold;
    }
    
    .buttons {
      display: flex;
      gap: 0.75rem;
    }
    
    button {
      flex: 1;
      padding: 0.75rem 1rem;
      border: none;
      border-radius: 8px;
      font-size: 0.9375rem;
      font-weight: 500;
      cursor: pointer;
      transition: background-color 0.15s ease;
    }
    
    .btn-approve {
      background: var(--primary-color);
      color: white;
    }
    
    .btn-approve:hover {
      background: var(--primary-hover);
    }
    
    .btn-deny {
      background: var(--bg-color);
      color: var(--text-color);
      border: 1px solid var(--border-color);
    }
    
    .btn-deny:hover {
      background: var(--border-color);
    }
    
    .footer {
      text-align: center;
      margin-top: 1.5rem;
      font-size: 0.75rem;
      color: var(--text-muted);
    }
  </style>
</head>
<body>
  <div class="consent-card">
    <div class="header">
      <div class="app-icon"><%= HTML.escape(client_name[0..1].upcase) %></div>
      <h1><%= HTML.escape(client_name) %></h1>
      <p class="subtitle">wants to access your account</p>
      <div class="user-badge"><%= HTML.escape(user_email) %></div>
    </div>
    
    <div class="permissions">
      <p class="permissions-title">This application will be able to:</p>
      <ul>
        <% if scopes.empty? %>
        <li>Access your basic profile information</li>
        <% else %>
        <% scopes.each do |scope| %>
        <li><%= HTML.escape(scope) %></li>
        <% end %>
        <% end %>
      </ul>
    </div>
    
    <form method="POST" action="/oauth/consent">
      <input type="hidden" name="client_id" value="<%= HTML.escape(client_id) %>">
      <input type="hidden" name="scope" value="<%= HTML.escape(scope) %>">
      <input type="hidden" name="state" value="<%= HTML.escape(state) %>">
      <input type="hidden" name="redirect_uri" value="<%= HTML.escape(redirect_uri) %>">
      
      <div class="buttons">
        <button type="submit" name="decision" value="deny" class="btn-deny">Deny</button>
        <button type="submit" name="decision" value="approve" class="btn-approve">Approve</button>
      </div>
    </form>
    
    <div class="footer">
      Authorizing will redirect you to <%= HTML.escape(redirect_host) %>
    </div>
  </div>
</body>
</html>
